<% set_meta_tags :site => 'Verify Bank • Shift',
  :noindex => true
%>

<div class="row">
  <div class="col-md-6 col-md-offset-1">
    <div class="signup">
      <h5>Confirm Bank</h5>
      <%= render "layouts/messages" %>
      <%= form_for (@account), :url => {controller: "accounts", action: "update", id: @account.id, user_id: @user.id}, :html => { :method => :put} do |f| %>
        <%= form_errors @account %>
        <fieldset class="form-group">
          <label>Routing Number</label>
          <%= f.text_field :bank_routing_number, class: "form-control", require: true %>
        </fieldset>
        <fieldset class="form-group">
          <label>Account Number</label>
          <%= f.text_field :bank_account_number, class: "form-control", require: true %>
        </fieldset>
        <div class="spacer -space-16"></div>
        <%= f.submit "Verify", class: "button -blue -full" %>
      <% end %>
    </div>
  </div>
  <div class="col-md-4">
    <h6 class="help-header">Added Security</h6>
    <p>For additional security we need to verify your bank account with two micro-deposits.</p>
    <p>First confirm your routing and account numbers, then we will depost two small amounts, and finish by confirming those amounts in 1-3 days.</p>
    <a href="http://support.shiftsavings.com/signing-up/micro-deposits" target="_blank" class="help-link"><i class="fa fa-question"></i> Support: Micro-deposits</a>
    <p class="additional-help">Need additional help? Chat with us below or <a href="https://www.shiftsavings.com/contact" target="_blank">send us an email</a> with your question.</p>
  </div>
</div>
