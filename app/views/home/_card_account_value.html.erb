<% if @biz_owner.nil? %>
  <div class="milo-card account-value">
    <div class="mc-title">Account Value</div>
    <%= line_chart @transfer_stats.group_by_day(:date).maximum(:roundup_amount) %>
  </div>
<% end %>
