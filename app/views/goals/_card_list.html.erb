<div class="milo-card card-goals">
  <% if @goals.present? %>
    <div class="mc-title">
      <div class="title-add-goal" data-toggle="modal" data-target="#setGoal">Add A Goal</div>
      Savings Goals
    </div>
    <% @goals.each do |g| %>
      <div class="goal-item">
        <div class="gi-title">
          <a class="gi-edit" data-toggle="modal" data-target="#editGoal"><i class="fa fa-pencil"></i></a>
          <span><%= @user.account_balance ? number_to_currency(@user.account_balance / 100.00) : "$0.00" %> / $<%= g.amount %></span>
          <%= g.name %>
        </div>
        <div class="gi-progress-bar">
          <div class="current-progress" style="width: 75%"></div>
          <div class="bar -b1"></div>
          <div class="bar -b2"></div>
          <div class="bar -b3"></div>
          <div class="bar -b4"></div>
          <div class="bar -b5"></div>
          <div class="bar -b6"></div>
          <div class="bar -b7"></div>
          <div class="bar -b8"></div>
          <div class="bar -b9"></div>
        </div>
      </div>
    <% end %>
  <% else %>
    <div class="mc-title">Create a Goal</div>
    <p>Make a bigger impact and set a goal to motivate yourself to save for an important financial goal like a college education, new home or vacation.</p>
    <button type="button" class="button -blue" data-toggle="modal" data-target="#setGoal">Set Goal</button>
  <% end %>
</div>
